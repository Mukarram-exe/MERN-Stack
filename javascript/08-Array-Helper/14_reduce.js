const arr = [22,44,55,66,99,100];
// sum of all elements in array using declerative funciton.
// let sum = 0;
// arr.forEach(ele => sum += ele );
// console.log(sum);
let output = arr.reduce((previousValue, currentValue)=>{
    console.log(previousValue,currentValue);
    
})
